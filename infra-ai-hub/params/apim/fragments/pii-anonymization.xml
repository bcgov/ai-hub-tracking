<fragment>
    <!-- ================================================================== -->
    <!-- PII Anonymization — External Redaction Service (Routing Only)     -->
    <!-- ================================================================== -->
    <!--
     Routes all PII-enabled requests to the external pii-redaction Container
     App. APIM acts as a policy enforcement point only; all Language Service
     calls are handled by the external service.

     Input variables (set by caller before <include-fragment>):
       piiInputContent:         Request body as JSON string (messages object)
       piiAnonymizationEnabled: "true" to enable; any other value = pass-through
       piiExcludedCategories:   JSON array of PII category strings to exclude
       piiDetectionLanguage:    Language code for detection (default "en")
       piiFailClosed:           "true" = block request on failure (503)
                                "false"/absent = pass original body on failure
       piiScanRoles:            JSON array of message roles to scan
       piiExternalRedactionUrl: Base URL of the pii-redaction Container App

     Output variable (consumed by caller after <include-fragment>):
       piiAnonymizedContent:    Redacted body JSON string, or original on
                                fail-open / PII disabled
    -->
    <choose>
        <when condition="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiAnonymizationEnabled&quot;, &quot;false&quot;) == &quot;true&quot;)">
            <set-variable name="piiStartTimeTicks" value="@(DateTime.UtcNow.Ticks)" />
            <!-- Send request to the pii-redaction Container App -->
            <send-request mode="new" response-variable-name="piiExternalResponse" timeout="60" ignore-error="true">
                <set-url>@(context.Variables.GetValueOrDefault&lt;string&gt;("piiExternalRedactionUrl", "") + "/redact")</set-url>
                <set-method>POST</set-method>
                <set-header name="Content-Type" exists-action="override">
                    <value>application/json</value>
                </set-header>
                <set-body>@{
                    var inputText  = context.Variables.GetValueOrDefault&lt;string&gt;("piiInputContent", "{}");
                    var excCats    = context.Variables.GetValueOrDefault&lt;string&gt;("piiExcludedCategories", "[]");
                    var language   = context.Variables.GetValueOrDefault&lt;string&gt;("piiDetectionLanguage", "en");
                    var failClosed = context.Variables.GetValueOrDefault&lt;string&gt;("piiFailClosed", "false") == "true";
                    var scanRoles  = context.Variables.GetValueOrDefault&lt;string&gt;("piiScanRoles", "[\"user\",\"assistant\",\"tool\"]");

                    JObject bodyObj;
                    try { bodyObj = JObject.Parse(inputText); } catch { bodyObj = new JObject(); }

                    JArray excludedArr;
                    try { excludedArr = JArray.Parse(excCats); } catch { excludedArr = new JArray(); }

                    JArray scanRolesArr;
                    try { scanRolesArr = JArray.Parse(scanRoles); }
                    catch { scanRolesArr = new JArray(new[] { "user", "assistant", "tool" }); }

                    return new JObject(
                        new JProperty("body", bodyObj),
                        new JProperty("config", new JObject(
                            new JProperty("fail_closed",         failClosed),
                            new JProperty("excluded_categories", excludedArr),
                            new JProperty("detection_language",  language),
                            new JProperty("scan_roles",          scanRolesArr),
                            new JProperty("correlation_id",      context.RequestId.ToString())
                        ))
                    ).ToString(Newtonsoft.Json.Formatting.None);
                }</set-body>
            </send-request>
            <!-- Capture status code and elapsed time for diagnostics -->
            <set-variable name="piiDetectionStatusCode" value="@{
                try {
                    var r = context.Variables.GetValueOrDefault&lt;IResponse&gt;(&quot;piiExternalResponse&quot;);
                    return r != null ? ((int)r.StatusCode).ToString() : &quot;0&quot;;
                } catch { return &quot;0&quot;; }
            }" />
            <set-variable name="piiDurationMs" value="@{
                var ticks = context.Variables.GetValueOrDefault&lt;long&gt;(&quot;piiStartTimeTicks&quot;, DateTime.UtcNow.Ticks);
                return ((DateTime.UtcNow.Ticks - ticks) / TimeSpan.TicksPerMillisecond).ToString();
            }" />
            <!-- Read response body once (Body.As<string> with preserveContent avoids stream exhaustion) -->
            <set-variable name="piiExternalBodyStr" value="@{
                var code = context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiDetectionStatusCode&quot;, &quot;0&quot;);
                if (code != &quot;200&quot;) { return &quot;&quot;; }
                try {
                    var r = context.Variables.GetValueOrDefault&lt;IResponse&gt;(&quot;piiExternalResponse&quot;);
                    return r != null ? r.Body.As&lt;string&gt;(preserveContent: true) : &quot;&quot;;
                } catch { return &quot;&quot;; }
            }" />
            <!-- Success: HTTP 200, status=="ok", full_coverage==true, redacted_body present -->
            <choose>
                <when condition="@{
                    try {
                        var code = context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiDetectionStatusCode&quot;, &quot;0&quot;);
                        if (code != &quot;200&quot;) { return false; }
                        var body = context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiExternalBodyStr&quot;, &quot;&quot;);
                        if (string.IsNullOrEmpty(body)) { return false; }
                        var obj = JObject.Parse(body);
                        return obj[&quot;status&quot;]?.ToString() == &quot;ok&quot;
                            &amp;&amp; obj[&quot;full_coverage&quot;]?.Value&lt;bool&gt;() == true
                            &amp;&amp; obj[&quot;redacted_body&quot;] != null;
                    } catch { return false; }
                }">
                    <set-variable name="piiAnonymizedContent" value="@{
                        var body = context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiExternalBodyStr&quot;, &quot;&quot;);
                        return JObject.Parse(body)[&quot;redacted_body&quot;].ToString(Newtonsoft.Json.Formatting.None);
                    }" />
                    <set-variable name="piiContentChanged" value="@{
                        var orig = context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiInputContent&quot;, &quot;&quot;);
                        var anon = context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiAnonymizedContent&quot;, &quot;&quot;);
                        return (!string.IsNullOrEmpty(anon) &amp;&amp; orig != anon) ? &quot;true&quot; : &quot;false&quot;;
                    }" />
                    <set-variable name="piiDiagnostics" value="@{
                        try {
                            var body = context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiExternalBodyStr&quot;, &quot;&quot;);
                            var obj = JObject.Parse(body);
                            return (obj[&quot;diagnostics&quot;] ?? new JObject()).ToString(Newtonsoft.Json.Formatting.None);
                        } catch { return &quot;{}&quot;; }
                    }" />
                    <trace source="pii-anonymization" severity="information">
                        <message>PII redaction succeeded</message>
                        <metadata name="event-type" value="pii-anonymization" />
                        <metadata name="request-id" value="@(context.RequestId.ToString())" />
                        <metadata name="subscription-id" value="@(context.Subscription.Id)" />
                        <metadata name="pii-status-code" value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiDetectionStatusCode&quot;, &quot;0&quot;))" />
                        <metadata name="pii-duration-ms" value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiDurationMs&quot;, &quot;0&quot;))" />
                        <metadata name="pii-redaction-succeeded" value="true" />
                        <metadata name="pii-content-changed" value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiContentChanged&quot;, &quot;false&quot;))" />
                        <metadata name="pii-fail-closed" value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiFailClosed&quot;, &quot;false&quot;))" />
                        <metadata name="pii-coverage-full" value="true" />
                        <metadata name="pii-entity-count" value="@{
                            try { return JObject.Parse(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiDiagnostics&quot;, &quot;{}&quot;))[&quot;entity_count&quot;]?.ToString() ?? &quot;0&quot;; } catch { return &quot;0&quot;; }
                        }" />
                        <metadata name="pii-document-count" value="@{
                            try { return JObject.Parse(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiDiagnostics&quot;, &quot;{}&quot;))[&quot;total_docs&quot;]?.ToString() ?? &quot;0&quot;; } catch { return &quot;0&quot;; }
                        }" />
                    </trace>
                </when>
                <!-- Failure: network error, non-200, or service could not fully redact -->
                <otherwise>
                    <set-variable name="piiFailureReason" value="@{
                        var code = context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiDetectionStatusCode&quot;, &quot;0&quot;);
                        if (code == &quot;0&quot;) { return &quot;no-response&quot;; }
                        if (code == &quot;413&quot;) { return &quot;payload-too-large&quot;; }
                        if (code != &quot;200&quot;) { return &quot;http-&quot; + code; }
                        try {
                            var body = context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiExternalBodyStr&quot;, &quot;&quot;);
                            var obj  = JObject.Parse(body);
                            var svcErr = obj[&quot;error&quot;]?.ToString() ?? &quot;&quot;;
                            return string.IsNullOrEmpty(svcErr) ? &quot;incomplete-coverage&quot; : (&quot;service-error: &quot; + svcErr);
                        } catch { return &quot;incomplete-coverage&quot;; }
                    }" />
                    <trace source="pii-anonymization" severity="warning">
                        <message>PII redaction failed</message>
                        <metadata name="event-type" value="pii-anonymization" />
                        <metadata name="request-id" value="@(context.RequestId.ToString())" />
                        <metadata name="subscription-id" value="@(context.Subscription.Id)" />
                        <metadata name="pii-status-code" value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiDetectionStatusCode&quot;, &quot;0&quot;))" />
                        <metadata name="pii-duration-ms" value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiDurationMs&quot;, &quot;0&quot;))" />
                        <metadata name="pii-redaction-succeeded" value="false" />
                        <metadata name="pii-fail-closed" value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiFailClosed&quot;, &quot;false&quot;))" />
                        <metadata name="pii-failure-reason" value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiFailureReason&quot;, &quot;unknown&quot;))" />
                    </trace>
                    <choose>
                        <when condition="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiFailClosed&quot;, &quot;false&quot;) == &quot;true&quot;)">
                            <return-response>
                                <set-status code="503" reason="Service Unavailable" />
                                <set-header name="Content-Type" exists-action="override">
                                    <value>application/json</value>
                                </set-header>
                                <set-header name="X-Request-Id" exists-action="override">
                                    <value>@(context.RequestId.ToString())</value>
                                </set-header>
                                <set-body>@{
                                    var reason = context.Variables.GetValueOrDefault&lt;string&gt;("piiFailureReason", "unknown");
                                    return new JObject(
                                        new JProperty("error", new JObject(
                                            new JProperty("code",           "PiiRedactionFailed"),
                                            new JProperty("message",        "PII redaction could not be completed and fail-closed policy is in effect."),
                                            new JProperty("request_id",     context.RequestId.ToString()),
                                            new JProperty("failure_reason", reason)
                                        ))
                                    ).ToString(Newtonsoft.Json.Formatting.None);
                                }</set-body>
                            </return-response>
                        </when>
                        <otherwise>
                            <!-- Fail-open: pass original unredacted body through -->
                            <set-variable name="piiAnonymizedContent"
                                value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiInputContent&quot;, &quot;{}&quot;))" />
                        </otherwise>
                    </choose>
                </otherwise>
            </choose>
        </when>
        <!-- PII disabled: pass original body through unchanged -->
        <otherwise>
            <set-variable name="piiAnonymizedContent"
                value="@(context.Variables.GetValueOrDefault&lt;string&gt;(&quot;piiInputContent&quot;, &quot;{}&quot;))" />
        </otherwise>
    </choose>
</fragment>
